# Telegram-уведомления для AI dev loop

Оркестратор сам пишет вам в Telegram, когда нужно ваше внимание.
Смотреть на сервер больше не нужно.

## Когда бот вас дёргает

- 🛑 3 попытки исчерпаны, CI красный — нужна помощь (+ ссылка на PR)
- 🛑 Агент не сделал ни одного коммита (задача сформулирована непонятно)
- 🛑 Оркестратор упал с ошибкой
- 👀 PR готов и ждёт вашего ревью
- ✅ Задача ушла в авто-merge (если AUTO_MERGE=true)

Когда бот молчит (это нормально): очередь пуста; Claude упёрся в лимит
подписки (задача сама вернётся в очередь — если пинговать и об этом,
бот будет спамить каждые 15 минут, пока лимит не откроется).

## Настройка — 5 минут

**Шаг 1. Создать бота.** В Telegram откройте @BotFather → `/newbot` →
придумайте имя → получите токен вида `1234567890:AAE...`.

**Шаг 2. «Познакомиться» с ботом.** Найдите своего бота по имени и
нажмите Start (или напишите ему что угодно) — иначе он не сможет
писать вам первым.

**Шаг 3. Узнать свой chat_id.** На любой машине:

```bash
curl -s "https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates" \
  | jq '.result[-1].message.chat.id'
```

Число (например `123456789`) — это и есть ваш chat_id.

**Шаг 4. Добавить в конфиг.** В `/etc/ai-dev.env` две строки:

```bash
TELEGRAM_BOT_TOKEN=1234567890:AAE...
TELEGRAM_CHAT_ID=123456789
```

(Файл уже должен быть с правами 600 — проверьте: `ls -l /etc/ai-dev.env`.)

**Шаг 5. Проверить.** С сервера:

```bash
source /etc/ai-dev.env
curl -s "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
  -d chat_id="${TELEGRAM_CHAT_ID}" \
  --data-urlencode text="✅ AI dev loop: уведомления работают"
```

Пришло сообщение — готово. Больше ничего перезапускать не нужно:
оркестратор подхватит переменные при следующем запуске таймера.

## Безопасность токена

- Токен живёт ТОЛЬКО в `/etc/ai-dev.env` (права 600). Не вставляйте
  его в issues, промпты или код.
- Агенту токен не виден: оркестратор вычищает `TELEGRAM_BOT_TOKEN` и
  `TELEGRAM_CHAT_ID` из окружения перед каждым запуском Claude
  (`env -u ...`). Уведомления шлёт только сам скрипт-оркестратор.
- Если токен утёк — в @BotFather: `/revoke` → бот получит новый токен,
  старый умрёт. Обновите env-файл.

## Если сообщения не приходят

1. Проверьте шаг 2 — боту нужно хотя бы одно ваше сообщение.
2. `getUpdates` вернул пусто — напишите боту ещё раз и повторите.
3. Сервер не достаёт до `api.telegram.org` (сетевые блокировки) —
   понадобится прокси для Bot API; напишите мне, добавим поддержку
   в функцию tg().
